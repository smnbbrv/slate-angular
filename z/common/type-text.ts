import { ZText } from "z/types/text";
import { Transaction } from "./transaction";

export function typeTextInsertGenerics(transaction: Transaction, parent: ZText, index: number, text: string) {
    
}